# PPARS — Placement Preparation Analytics & Repository System

A minimal, production-like project aligned to Scaler Lo/Co intern role.

## Quickstart

```bash
cd backend
python -m venv .venv && source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
python seed.py
export FLASK_APP=app:app && flask run  # Windows: set FLASK_APP=app:app && flask run
```
